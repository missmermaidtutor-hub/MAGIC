/**
 * Web font loader — Montserrat is loaded via CSS in index.html.
 * Always returns true so the app renders immediately.
 * Expo automatically uses this file instead of useAppFonts.js on web.
 */
export default function useAppFonts() {
  return true;
}
